3.GLFW
  3.1 GLFW 的作用
    GLFW 是一个跨平台窗口与输入管理库，主要用于：
    • 创建窗口
    • 创建和管理 OpenGL Context
    • 处理键盘、鼠标等输入
    • 处理窗口事件
    • 交换 Front Buffer / Back Buffer
    • 屏蔽 Windows、Linux、macOS 之间的部分平台差异
    -----------------------------------------------
    应用程序
       ↓
     GLFW
       ↓
    Windows / Linux / macOS
    开发者只调用统一的 GLFW API，不需要分别编写 Windows、Linux、macOS 的窗口代码
    ------------------------------------------------

  3.2 GLFW 编译
    GLFW 可以直接使用预编译库，也可以从源码自行编译。
    本课程使用的是官网预编译后的静态库，无需源代码

  3.3 GLFW 配置
    -----------------------------------------------
    项目结构：
    CG!\
    ├─ Include\
    │  └─ GLFW\
    │     ├─ glfw3.h
    │     └─ glfw3native.h
    │
    ├─ Lib\
    │  └─ glfw3.lib
    │
    ├─ Dll\
    │
    └─ CG!\
       └─ main.cpp
    -------------------------------------------------
      3.31 配置头文件
        Visual Studio：
        项目属性
        → C/C++
        → 常规
        → 附加包含目录
        
        添加：
        ..\Include
    --------------------------------------------------
        于是代码可以写：
        #include <GLFW/glfw3.h>
    --------------------------------------------------
      3.1.2 配置库目录
        项目属性
        → 链接器
        → 常规
        → 附加库目录
        
        添加：
        ..\Lib
    ---------------------------------------------------
      3.1.3 配置链接库
        项目属性
        → 链接器
        → 输入
        → 附加依赖项
        
        添加：
        glfw3.lib
        opengl32.lib
    ----------------------------------------------------
        其中：
        glfw3.lib → GLFW 静态库
        opengl32.lib → Windows SDK 提供的 OpenGL 系统库
  
  3.4GLFW初始化
    复制并编译官方function calls代码
    官方网址：https://www.glfw.org/
