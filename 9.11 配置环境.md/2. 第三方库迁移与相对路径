1.第三方库迁移是什么？（核心思想）
    源代码只负责写“要使用什么”
    Visual Studio 配置负责告诉编译器/链接器“去哪里找”
    简而言之即：第三方库迁移 = 将 Include、Lib、Dll 整合进项目，并用相对路径配置，使库位置变化不影响源代码。
    因此 只需要项目内部目录结构保持不变 整个文件夹可以移动到其他磁盘或目录，相对路径仍然有效

1.2 优点
    避免写死绝对路径
    项目移动后通常无需重新配置
    方便提交作业和迁移到其他电脑
    降低路径错误和环境依赖

1.3 举例理解
    项目中统一保存：
    CG!\
    ├─ Include\   # 头文件
    ├─ Lib\       # .lib
    ├─ Dll\       # .dll
    └─ CG!\       # 源代码
  ---------------------------------------------
    Visual Studio 使用相对路径配置：
    C/C++ → 附加包含目录：
    ..\Include
    
    链接器 → 附加库目录：
    ..\Lib
    
    调试 → 环境：
    PATH=$PATH$;..\Dll
  -----------------------------------------------
    因此代码仍然可以保持：
    #include <GL/glew.h>
    #include <GLFW/glfw3.h>
    不需要因为库从 D:\xxx 移动到项目文件夹而修改。
  ------------------------------------------------

2.相对路径
    相对路径表示：路径不是从磁盘根目录写起，而是相对于当前目录的位置关系来定位文件。
    eg：..\Include
    绝对路径表示：从根目录开始，完整、唯一定位到文件 / 文件夹的全部路径，不依赖你当前在哪，永远指向同一个位置
    eg：D:\Courses\ComputerGraphics\CG!\Include

2.1 优点
    只依赖项目内部目录结构，不依赖具体磁盘和存放位置。

2.2举例理解
    例如项目结构为：
    CG!\
    ├─ CG!\
    ├─ Include\
    ├─ Lib\
    └─ Dll\
    如果当前项目目录是：
    CG!\CG!\
      那么：
      ..\Include
      表示：
      ..        → 返回上一级
      Include   → 进入 Include 文件夹
    即最终定位到：
    CG!\Include
    ------------------------------------------------
    常用写法：
    .\表示当前目录
    ..\表示上一级目录
    ..\Include表示返回上一级后进入 Include
    ------------------------------------------------
    注意：路径名尽量不要包含空格、中文、路径符号、特殊字符










